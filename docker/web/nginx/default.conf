# Nginx configuration
server {
    listen 80;
    server_name _;
    root /app/;

    index index.php index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    include snippets/php.conf;
}
